<div>
  <h4>Comentário por <%=link_to comment.author_name, user_path(comment.author_name) %></h4>
  <p class="comment"><%= raw comment.body %></p>


<% if user_signed_in? %>
<% if current_user.username == comment.author_name or current_user.admin? %>
<%= link_to 'Excluir', [comment.article, comment],
               method: :delete,
               data: { confirm: 'Tem certeza?' } %>
<%= link_to 'Edit', edit_article_comment_path(@article, comment) %>
<% end %><% end %>
</p>
</div>

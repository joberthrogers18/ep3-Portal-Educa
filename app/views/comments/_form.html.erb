<%= form_for [ @article, @comment ] do |f| %>

  <p>
    <%= f.label :corpo %><br/>
    <%= f.cktext_area :body %>
  </p>
  <p>
    <%= f.submit 'Enviar', :class=>"waves-effect waves-light btn" %>
  </p>
<% end %>
